<!-- Nav: ← [AI Skills Spec](ai-skills-spec.md) | → [ai_skills_context.py](../custom_components/climate_advisor/ai_skills_context.py) | ↔ Related: [Chart Log Spec](chart-log-spec.md) [Computation Reference](08-COMPUTATION-REFERENCE.md) -->

# Activity Timeline — Per-Event Table Spec (Tier 3)

_Introduced: Issue #330 (deterministic table) · Chart Vent bar: Issue #331 · Relocated from
`ai_skills_activity.py` to `ai_skills_context.py` when the activity_report/investigator skills
merged (Issue #563) — the table itself, the renderer catalog, and the dedup logic below are
unchanged by that move, only the module and calling mechanism changed._

## Anchors

| Question | Short answer | → Full answer |
|---|---|---|
| How is the per-event table built — LLM or Python? | Deterministically in Python by `build_event_timeline_table()` in `ai_skills_context.py` (moved from `ai_skills_activity.py`, Issue #563). Not LLM-generated. The AI writes the other narrative sections of the merged investigator skill's output. | [Overview](#overview) |
| What do the Event, Settings, Indoor, and Outdoor columns mean? | Event = what happened. Settings = the concrete HVAC action (setpoint, mode, fan state). Indoor/Outdoor = ambient temperatures at event emit time, sourced from coordinator.data; `—` for events recorded before Issue #352. | [Overview](#overview) |
| How does `_format_band_setpoint` render a setpoint? | Active edge first (the live thermostat setpoint), other edge in parens as the monitored bound: `setpoint: 72°F Cool (64°F Heat)`. | [Setpoint Convention](#setpoint-convention) |
| What is the full set of event types the table must handle? | 41 registered types across 6 groups: band/setpoint-program, setpoint/mode delta, override lifecycle, nat-vent/fan, skip/advisory, system/diagnostic. | [Event Catalog](#event-catalog) |
| What happens when an unrecognised event type appears? | `_default_renderer` fires: humanized type name + `reason` field as Event; generic field extraction as Settings. Never blank, never crashes. | [Default Renderer](#default-renderer) |
| How does the table handle many consecutive band rows of the same type? | Consecutive same-type rows collapse to `{name} ×N ({start}–{end})` while preserving the shared Settings cell. | [Dedup Contract](#dedup-contract) |
| How do I add or change an event renderer? | Four steps: emit with structured fields, add/update `EVENT_RENDERERS` entry, add catalog row here, add unit test. | [Runbook: Adding or Changing a Renderer](#runbook-adding-or-changing-a-renderer) |
| What test ensures a new event type is never silently blank? | The coverage-guardrail test in `tests/test_activity_renderers.py` (`TestEventRenderersCoverage`) introspects both emitters for all event type literals and asserts each is in `EVENT_RENDERERS` or documented as intentionally default-rendered. | [Coverage Guardrail](#coverage-guardrail) |
| How do I hide an event type from the report entirely (not just relabel it)? | Only for events with no independent occupant-facing meaning (e.g. `unprotected_grace_started`, a pure diagnostic signal that would otherwise duplicate its sibling `grace_started` row). Add the type to `_REPORT_HIDDEN_EVENT_TYPES` in `ai_skills_context.py` — the raw event log is untouched, only the rendered row disappears. | [Default Renderer](#default-renderer) |

---

## Overview

The **activity timeline** is a two-column markdown table rendered by
`build_event_timeline_table(raw_event_log, config, hours, now)` in
`ai_skills_context.py`. It is **deterministic Python** — not generated by the LLM.

**Mechanism changed at Issue #563** (skill merge). Previously, the LLM produced a `##
TIMELINE` section that got parsed and then *overridden* post-hoc with this table's output.
The merged `investigator` skill doesn't ask the model to produce a timeline at all — the
table is supplied directly as its own `activity_timeline` context provider (registered in
`ai_skills_context.py`'s `ContextProviderRegistry`), read by the model as input evidence,
not written by it as output. There is no `sections["timeline"]` override step to reason
about; the table simply never passes through the model.

The same table renders identically whether or not Claude is reachable — it's built from
`coordinator._event_log` directly, independent of the AI call path, so it's available even
when `investigation_fallback()` (the deterministic fallback) is what actually runs.

**Column semantics:**

| Column | What it contains |
|---|---|
| **Time** | Local HH:MM of the event |
| **Event** | What happened — a human-readable label for the trigger or action type |
| **Settings** | The concrete HVAC action performed — setpoint, mode change, fan state, or skip reason |
| **Indoor** | Indoor temperature at the moment the event was emitted, from `coordinator._get_indoor_temp()`. Displays `—` for events recorded before Issue #352 enrichment. |
| **Outdoor** | Outdoor temperature at the moment the event was emitted, from `coordinator._last_outdoor_temp`. Same availability caveat. |

The split between Event and Settings lets users scan the Settings column to understand
_what the thermostat was told to do_ independently from the event labels. Indoor and
Outdoor make the ambient conditions visible alongside each decision, eliminating the
need to cross-reference chart data to understand why a setpoint was applied.

---

## Setpoint Convention

Climate Advisor operates **single-setpoint** (Issue #301). At any moment the
thermostat holds exactly one active setpoint; the other comfort edge is the monitored
bound that would trigger the next action.

`_format_band_setpoint(floor, ceiling, active, unit)` renders both values:

| `active` value | Rendered string |
|---|---|
| `"ceiling"` | `setpoint: {ceiling} Cool ({floor} Heat)` |
| `"floor"` | `setpoint: {floor} Heat ({ceiling} Cool)` |

- The **first value** is the live thermostat setpoint (`climate.set_temperature` was
  called with this value).
- The **parenthesised value** is the monitored bound — the edge that, if crossed, would
  trigger the next action.

**Examples:**

- `active="ceiling"`, `ceiling=72°F`, `floor=64°F` →
  `setpoint: 72°F Cool (64°F Heat)`
- `active="floor"`, `floor=64°F`, `ceiling=72°F` →
  `setpoint: 64°F Heat (72°F Cool)`

Temperature values are formatted by `temperature.format_temp(value, unit)` to respect
the user's configured unit (°F or °C).

---

## Event Catalog

One row per emitted event type. Emitters are `automation.py` (via
`_emit_event_callback`) and `coordinator.py` (via `_emit_event`).

Grouped by functional area. 36 registered types total.

### Band / Setpoint-Program

These events signal a comfort band change and always carry `floor`, `ceiling`, `active`
fields. Settings = `_format_band_setpoint(floor, ceiling, active, unit)`.

| Event type | Emitter | Key payload fields | Event-column text | Settings-column rule |
|---|---|---|---|---|
| `comfort_band_applied` | `automation.py` `_apply_comfort_band` | `floor`, `ceiling`, `active`, `mode`, `reason` | "Comfort band applied" | `_format_band_setpoint(floor, ceiling, active, unit)` — **excluded from dedup** (`_NO_DEDUP`): each application carries a distinct setpoint payload (#330). |
| `bedtime_setback` | `automation.py` `handle_bedtime` | `mode`, `floor`, `ceiling`, `active`, `modifier` | "Bedtime setback — sleep band" | `_format_band_setpoint(floor, ceiling, active, unit)` — **excluded from dedup** (`_NO_DEDUP`). |
| `morning_wakeup` | `automation.py` `handle_morning_wakeup` | `mode`, `floor`, `ceiling`, `active` | "Morning wake-up — daytime band" | `_format_band_setpoint(floor, ceiling, active, unit)` — **excluded from dedup** (`_NO_DEDUP`). |
| `occupancy_setback` | `automation.py` `handle_occupancy_away` / `handle_occupancy_vacation` | `mode` (away/vacation), `floor`, `ceiling`, `occupancy` | "Occupancy setback — {occupancy} mode" | `_format_band_setpoint(floor, ceiling, active="ceiling", unit)` (ceiling preferred; floor used when active is explicit) — **dedup-eligible** (not in `_NO_DEDUP`, Issue #485): the emitter has no dedup of its own (unlike `comfort_band_applied`'s #444 time-windowed dedup), and re-fires every automation cycle while occupancy stays away/vacation, so consecutive identical rows collapse to `×N` per the [Dedup Contract](#dedup-contract). |
| `occupancy_comfort_restored` | `automation.py` `handle_occupancy_home` | `mode`, `target_f` | "Occupancy — comfort restored" | `setpoint: {target_f} {mode.title()}` |
| `pre_cool_applied` | `automation.py` `handle_pre_cool` | `target`, `modifier`, `sleep_cool`, `floor`, `indoor`, `nat_vent_suppressed` | "Pre-cool — thermal mass banking" | `setpoint: {target}°F Cool (sleep floor {floor}°F Heat)` |

### Setpoint / Mode Delta

These events signal a mode or setpoint change triggered by automation logic.

| Event type | Emitter | Key payload fields | Event-column text | Settings-column rule |
|---|---|---|---|---|
| `override_detected` | `automation.py` `handle_thermostat_state_change` | `detected_mode`, `source`, `confirm_delay_seconds`, `old_mode`, `new_mode`, `old_setpoint_f`, `new_setpoint_f` | "Override detected — {source} ({confirm_delay_seconds//60} min confirm)" | `mode: {old_mode}→{new_mode}` if modes differ; `setpoint: {old_setpoint_f}→{new_setpoint_f}` if setpoint-only override |
| `ceiling_guard_fired` | `automation.py` `apply_classification` | `breach_time`, `hours_to_breach`, `lead_time_min`, `old_hvac_mode`, `new_hvac_mode`, `new_setpoint_f`, `old_setpoint_f` | "ODE ceiling guard — breach at {breach_time}" | `mode: {old_hvac_mode}→{new_hvac_mode}, setpoint: {new_setpoint_f}°F Cool` |
| `classification_applied` | `automation.py` `apply_classification` | `day_type`, `hvac_mode`, `trend`, `old_hvac_mode` | "Classification — {day_type} day ({trend})" | `mode: {old_hvac_mode}→{hvac_mode}` |
| `setpoint_rejected` | `automation.py` `_set_temperature` (validation callback) | `commanded`, `reported` | "Setpoint rejected by thermostat" | `commanded: {commanded}°F, thermostat reports: {reported}°F` |

### Override Lifecycle

| Event type | Emitter | Key payload fields | Event-column text | Settings-column rule |
|---|---|---|---|---|
| `override_cleared` | `automation.py` `clear_manual_override` (thermostat-mode override was active) OR `automation.py` `cancel_override` (Issue #508 — fan-only override cancelled, `was_mode`/`old_setpoint_f` both `None`) | `was_mode`, `active_since`, `old_setpoint_f` | "Manual override cleared" | `was: {was_mode} since {active_since HH:MM}` (blank fields render gracefully when `was_mode` is `None`) |
| `override_confirmed` | `automation.py` inner callback `_confirm_override_expired` (PATH A) | `mode`, `confirm_delay_seconds` | "Override confirmed — {mode}" | `grace: {confirm_delay_seconds//60} min delay elapsed` |
| `override_self_resolved` | `automation.py` inner callback (PATH B) | `detected_mode`, `current_mode` | "Override self-resolved (transient)" | blank |
| `grace_started` | `automation.py` `_start_grace_period` | `source`, `duration_seconds`, `trigger` | "Grace period started — {source}" | `duration: {duration_seconds//60} min, trigger: {trigger}` |
| `grace_expired` | `automation.py` `_on_grace_expired` | `source`, `re_paused` | "Grace period expired — {source}" | `re-paused: yes` if `re_paused` else `resuming normal control` |

### Nat-Vent / Fan

| Event type | Emitter | Key payload fields | Event-column text | Settings-column rule |
|---|---|---|---|---|
| `nat_vent_fan_on` | `automation.py` `_check_nat_vent_cycling` | `indoor_temp`, `on_threshold`, `target` | "Nat-vent cycling — fan on" | `fan: on (indoor {indoor_temp}°F ≥ {on_threshold}°F)` |
| `nat_vent_fan_off` | `automation.py` `_check_nat_vent_cycling` | `indoor_temp`, `off_threshold`, `target` | "Nat-vent cycling — fan off" | `fan: off (indoor {indoor_temp}°F ≤ {off_threshold}°F)` |
| `fan_activated` | `automation.py` `_activate_fan` | `reason`, `fan_mode` | "Fan activated — {reason}" | `fan: off->on` — every CA-commanded fan-on (min-runtime, economizer, whole-house, reconcile, thermostatic, nat-vent activation). Nat-vent cycling passes `emit_event=False` (uses `nat_vent_fan_on`). Issue #331. |
| `fan_deactivated` | `automation.py` `_deactivate_fan` | `reason`, `fan_mode` | "Fan deactivated — {reason}" | `fan: on->off` — sensor-closed + nat-vent-cycler paths pass `emit_event=False` (covered by `sensor_all_closed` / `nat_vent_fan_off`). |
| `fan_running_untracked` | `coordinator.py` `_async_update_data` | `hvac_action`, `fan_mode`, `thermostat_mode`, `source` | "Fan running (external) — {source}" (Issue #913: "untracked" → "external") | `fan: on (external; hvac_action={hvac_action})` — thermostat ran the fan CA did not command (e.g. AC blower between cool cycles). Deduped entry; paired `fan_untracked_cleared` on exit. Issue #331. |
| `fan_untracked_cleared` | `coordinator.py` `_async_update_data` | `fan_device` (optional) | "Fan stopped (external)" (Issue #913: "untracked fan ended" → "external") | `fan: {fan_device} off` if `fan_device` present, else `fan: off` — paired exit for `fan_running_untracked`. |
| `hvac_write_blocked_whf_active` | `automation.py` (`_set_temperature`/`_set_hvac_mode` choke-point guard, ~L3099/L3208) | `attempted_mode`, `reason` | "AC/heat blocked (whole-house fan active) -- {reason}" (Issue #913: "HVAC write blocked" → "AC/heat blocked") | `hvac: blocked ({attempted_mode})` — Issue #392 Fix 1b: the structural WHF/AC mutual-exclusion guard intercepted an HVAC write while WHF owns the thermostat, made visible here instead of silently dropping the write. Deduped 600s. |
| `whf_hvac_suppressed` | `automation.py` (WHF-session HVAC-suppress helper, ~L9779) | `prior_mode`, `reason` | "AC/heat paused (whole-house fan) -- {reason}" (Issue #913: "HVAC suppressed" → "AC/heat paused") | `hvac: {prior_mode}->off` — Issue #495: fires for both a CA-initiated and a manual/remote whole-house-fan session. |
| `whf_hvac_released` | `automation.py` (WHF-session release helper, ~L9850) | `reason` | "AC/heat resumed -- {reason}" (Issue #913: "HVAC suppression released" → "AC/heat resumed") | `hvac: reclassifying` — Issue #495: a manual/remote WHF session ended; CA's current classification is reasserted (not a blind restore of the mode captured at activation, since a remote-timer session can span hours). |
| `fan_manual_override` | `automation.py` `handle_fan_manual_override` | `fan_before`, `fan_after`, `fan_device`, `remote_speed` (optional), `remote_timer_hours` (optional) | "Fan manual override" | `{fan_device}: {fan_before}->{fan_after}` — plus `, remote: {remote_speed} speed` and/or `, remote timer: {remote_timer_hours}h` when the override was armed by an RF remote press with a speed/timer selection (Issue #524); a plain thermostat-detected override has neither field and omits both suffixes. |
| `fan_speed_observed` | `automation.py` `handle_fan_speed_observed` | `speed`, `fan_device` | "Fan speed observed (comfort-only)" | `{fan_device}: speed->{speed}` — a remote speed press while the fan was ALREADY running (Issue #519); deliberately NOT an override (no grace/HVAC-suppression armed) — see `fan_manual_override` for the off->on case. |
| `nat_vent_outdoor_rise_exit` | `automation.py` `_check_nat_vent_conditions` / `_check_nat_vent_cycling` | `outdoor`, `indoor` | "Nat-vent exit — outdoor warming" | `outdoor: {outdoor}°F ≥ indoor: {indoor}°F` |
| `nat_vent_comfort_floor_exit` | `automation.py` `_check_nat_vent_conditions` / `_check_nat_vent_cycling` | `indoor_temp`, `comfort_heat` | "Nat-vent exit — comfort floor reached" | `indoor: {indoor_temp}°F ≤ floor: {comfort_heat}°F; fan: off, heat restored` |
| `nat_vent_away_ceiling_exit` | `automation.py` `_check_nat_vent_conditions` | `indoor`, `comfort_cool` | "Nat-vent exit — away ceiling" | `indoor: {indoor}°F ≥ ceiling: {comfort_cool}°F` |
| `nat_vent_predicted_floor_exit` | `automation.py` `_check_nat_vent_conditions` | `time_to_floor_hr`, `fan_mode_change`, `hvac_mode_restored` | "Nat-vent proactive exit — floor predicted" | `floor in {time_to_floor_hr:.1f} hr; fan: {fan_mode_change}` |
| `nat_vent_bedtime_continue` | `automation.py` `handle_bedtime` | `outdoor_temp`, `sleep_cool` | "Nat-vent/WHF session active -- continuing through bedtime" | `outdoor: {outdoor_temp}°F, sleep ceiling: {sleep_cool}°F; fan continues (Issue #498 — no outdoor-vs-sleep_cool comparison is actually made, values are informational)` |
| `nat_vent_sleep_ceiling_reached` | `automation.py` `check_natural_vent_conditions` | `indoor_temp`, `sleep_cool` | "Nat-vent sleep ceiling reached" | `indoor: {indoor_temp}°F ≤ sleep target: {sleep_cool}°F; fan deactivated, sleep band retained` |
| `nat_vent_ceiling_escalation` | `automation.py` `apply_classification` | `indoor`, `outdoor`, `comfort_cool` | "Nat-vent escalated to AC — ceiling breached" | `indoor: {indoor}°F > ceiling: {comfort_cool}°F; nat-vent ended, AC armed` |
| `sensor_opened` | `automation.py` `handle_door_window_opened` | `entity`, `result` (natural_ventilation / paused), `trigger`, `hvac_mode_change`, optionally `fan_mode_change` | "Sensor opened — {entity}" with result label. **Special case (Issue #913):** when `entity == "natural_vent_reeval"` and `trigger == "open_door_reeval"` — a routine nat-vent re-check confirming a sensor that was already open, not a fresh open — the row reads `"Re-check: door/window confirmed open"` instead, so the occupant doesn't read it as a new door/window event. | `result: {result}; mode: {hvac_mode_change}` (+ `fan: {fan_mode_change}` if present); blank for the re-check special case unless mode/fan actually changed |
| `sensor_all_closed` | `automation.py` `handle_all_doors_windows_closed` | `was_paused`, `was_nat_vent`, `fan_device` (Issue #504) | "All sensors closed" if neither flag set; "All sensors closed -- ending nat-vent" if `was_nat_vent`; "All sensors closed -- resuming HVAC" if `was_paused` | Blank if neither flag; blank if `was_paused`; `{fan_device}: on->off` if `was_nat_vent` (Issue #504 — previously blank even though `_exit_nat_vent()` really did turn the fan off; `fan_deactivated`'s own event is intentionally suppressed here via `emit_event=False` per Issue #411, so this is the only place that transition can surface) |
| `stranded_hvac_suppression_restored` | `automation.py` `_deactivate_fan` | `reason`, `restore_mode` | "AC/heat restored (fan session ended) -- {reason}" (Issue #913: "Restored HVAC mode from stranded fan suppression" → "AC/heat restored (fan session ended)") | `hvac: ->{restore_mode}` — the fan was already physically inactive, but a `_pre_fan_hvac_mode` snapshot from an earlier session was still stranded; this restores the HVAC mode it captured. Issue #618: previously invisible in this table (only a DEBUG log line) — the specific corrective action that could silently cancel active/just-started cooling or heating (2026-08-10 incident) now has its own labeled entry. The sibling "fan was actively running and gets deactivated now" path already emits `fan_deactivated`; this is the one restore branch that historically emitted nothing at all. |
| `nat_vent_reconcile_exit` | `automation.py` (nat-vent reconcile handler, ~L6243) | `reason`, `fan_device` | "Nat-vent exit -- fan already running independently" (Issue #913: rewording of "fan found running without a CA-owned session") | `{reason}` (free-text reason string set at the emit site, passed through verbatim) — the reconcile path noticed the fan running with no CA-owned nat-vent session behind it and exits through the same pause/grace decision every other nat-vent exit site uses. |

### Skip / Advisory

These events carry no HVAC action — Settings is blank.

| Event type | Emitter | Key payload fields | Event-column text | Settings-column rule |
|---|---|---|---|---|
| `nat_vent_forecast_skip` | `automation.py` `handle_door_window_opened` | `forecast_peak`, `threshold` | "Nat-vent skipped — forecast too warm" | `peak: {forecast_peak}°F > threshold: {threshold}°F` |
| `nat_vent_floor_imminent_skip` | `automation.py` `handle_door_window_opened` | `time_to_floor_hr` | "Nat-vent skipped — floor imminent" | `floor in {time_to_floor_hr:.1f} hr (thermal model)` |
| `bedtime_setback_skipped` | `automation.py` `handle_bedtime` | `reason` (manual_override / occupancy), optionally `occupancy` | "Bedtime setback skipped — {reason}" | blank |
| `morning_wakeup_skipped` | `automation.py` `handle_morning_wakeup` | `reason` | "Morning wake-up skipped — {reason}" | blank |
| `pre_cool_suppressed_nat_vent` | `automation.py` `handle_pre_cool` | `indoor`, `target`, `modifier` | "Pre-cool suppressed — nat-vent achieved target" | `indoor: {indoor}°F ≤ target: {target}°F` |
| `pre_cool_overshoot` | `automation.py` `handle_morning_wakeup` | `indoor`, `comfort_heat` | "Pre-cool overshoot — indoor below comfort floor" | `indoor: {indoor}°F < comfort_heat: {comfort_heat}°F; heat may fire` |

### System / Diagnostic

| Event type | Emitter | Key payload fields | Event-column text | Settings-column rule |
|---|---|---|---|---|
| `system_restarted` | `coordinator.py` `async_restore_state` | `recovered_events` | "System restarted — HA restart boundary" | `{recovered_events} events recovered` |
| `startup_coalesced` | `coordinator.py` `_complete_startup_coalesce` | `nat_vent_activated`, `hvac_commanded`, `sensors_open_count` | "Startup coalescing complete" | `nat-vent: {nat_vent_activated}, HVAC: {hvac_commanded}, open sensors: {sensors_open_count}` |
| `stuck_grace_recovered` | `coordinator.py` `_async_update_data` (original: override stuck active past its own due grace-end-time — Issue #321) OR `coordinator.py` `_check_orphaned_grace` (mirror: grace active with no override behind it — Issue #508) | `grace_end_time`, `stale_mode`, `stale_since`, `reason` (only set by the Issue #508 call site, value `"grace_without_override"`) | "Grace period recovered (no override was active)" (`reason="grace_without_override"`) or "Grace period recovered (expired {grace_end_time})" (Issue #913: "Stuck grace recovered" → "Grace period recovered", dropped the "stuck" jargon) | `was {stale_mode} since {stale_since}` if `stale_mode` present, else blank |
| `state_contradiction_warning` | `coordinator.py` `_async_update_data` | `hvac_mode`, `hvac_action` | "Mode/action mismatch: {hvac_mode} vs {hvac_action}" (Issue #913: "State contradiction: mode=... but action=..." → "Mode/action mismatch: ... vs ...") | blank |
| `invariant_violation` | `coordinator.py` invariant check in `_async_update_data` (~L6398) | `invariant` (raw enum name, e.g. `AC_AND_WHF_BOTH_RUNNING`), `detail` (plain-English description — same text already shown on the dashboard Status card) | `{detail}` (Issue #913: previously led with the raw `invariant` enum name via `"Hard invariant violated: {invariant}"`; now leads with the already-plain-English `detail` field) | `invariant: {invariant}` — the raw enum code moved here from the Event column. Issue #749: a hard system invariant was violated (e.g. AC and the whole-house fan both physically running at once). Deduped 300s. |
| `comfort_family_switch_locked_out` | `automation.py` (comfort-family dwell-lockout guard, ~L8946; payload shape is a preserved contract with `comfort_family_fsm.py`, Issue #827 Design §2) | `candidate_family`, `reason` | "Switch to {candidate_family} delayed (recent change)" if `candidate_family` present, else "Switch delayed (recent change)" (Issue #913: "Family switch to {candidate} blocked by lockout" → "Switch to {candidate} delayed (recent change)") | `{reason}` — Issue #821: the family-switch lockout (`comfort_mode_switch_min_interval_s`) blocked a candidate heating/cooling family switch; surfaced so a persistently blocked breach is observable, not silent. |
| `unprotected_grace_started` | `automation.py` (non-protecting-trigger grace-start wrapper, ~L6482) | `source`, `trigger` | **Intentionally hidden from the Activity Report (Issue #913)** — no row is rendered for this event type. `build_event_timeline_table()` skips it via a hidden-event-types check near the top of the per-event loop; the raw event is untouched in `raw_event_log` and still feeds `coordinator.py`'s diagnostic override/grace FSM tracker (`_OVERRIDE_GRACE_FSM_EVENT_TYPE_MAP`). This matches Issue #672's original intent ("don't narrate the same grace-start twice") — the sibling `grace_started` event for the same action still renders normally. | N/A — no row rendered |
| `thermal_learning_no_observations` | `coordinator.py` `_handle_end_of_day` | `hvac_runtime_minutes` | "Thermal learning — no observations today" | `HVAC ran {hvac_runtime_minutes} min with zero thermal observations` |
| `incident_detected` | `automation.py` `_set_temperature` / `coordinator.py` `_emit_incident` | `incident_class`, `incident_id`, and class-specific fields | "Incident — {incident_class}" | class-specific fields formatted as `key: value` pairs |

---

## Default Renderer

Any event type **not registered** in `EVENT_RENDERERS`, and not in the hidden-types set
below, is handled by `_default_renderer(event_type, data)`:

- **Event column:** humanized type string (underscores → spaces, title-case) + `reason`
  field appended if present. Example: `"nat_vent_fan_on"` → `"Nat Vent Fan On"`.
- **Settings column:** generic extraction of recognized fields in this priority order:
  `old_setpoint_f`/`new_setpoint_f` → `setpoint: {old}→{new}`;
  `old_hvac_mode`/`new_hvac_mode` → `mode: {old}→{new}`;
  `floor`/`ceiling`/`active` → `_format_band_setpoint(...)`;
  `fan` → `fan: {fan}`;
  `trigger` → `trigger: {trigger}`;
  else blank.

**The default renderer never raises and never produces a blank Event column.** This
means a newly emitted event type renders meaningfully with zero code changes. Adding a
tailored renderer to `EVENT_RENDERERS` is opt-in.

**Hiding an event type entirely (Issue #913):** a small number of event types are pure
internal/diagnostic signals with no occupant-facing meaning of their own — e.g.
`unprotected_grace_started`, which exists only to feed `coordinator.py`'s diagnostic
override/grace FSM tracker and would otherwise duplicate the sibling `grace_started`
row for the same action. These are listed in `_REPORT_HIDDEN_EVENT_TYPES`
(`ai_skills_context.py`), checked at the top of `build_event_timeline_table()`'s
per-event loop — the event is skipped with a bare `continue` before any rendering runs.
The raw event is never removed from `raw_event_log`; only the rendered row disappears,
so nothing that reads the raw event log (the FSM tracker, `tools/sim_harness/outcomes.py`)
is affected. Use this only for events with no independent occupant-facing meaning — do
not use it to work around a jargon-y label that should instead be fixed via a renderer
(see the table above for the correct default: relabel, don't hide).

---

## Dedup Contract

Consecutive rows with the **same event type** are collapsed into a single row:

```
{friendly name} ×{N} ({first_HH:MM}–{last_HH:MM})
```

**The Settings cell is preserved** — all collapsed rows share the same band parameters
(they represent the same comfort state being re-armed), so the Settings text from the
first (or last) instance is used verbatim.

**Indoor and Outdoor cells use the first event in the collapsed run.** Temperature
conditions at the start of a repeated band cycle are more informative than the last.

This fixes the historical defect where `Sleep comfort band applied ×18` showed an empty
Settings column (Issue #330): the collapse preserves the `_format_band_setpoint` output
from the band's payload.

**Collapse applies only to consecutive same-type rows.** If event type A appears, then B
appears, then A appears again, the two A rows are not collapsed — they represent distinct
occurrences.

---

## Runbook: Adding or Changing a Renderer

Follow these four steps any time you add a new event emission or want to improve an
existing renderer.

### Step 1 — Emit with structured fields (not prose)

The event payload must carry all values needed to render the Settings column as discrete
fields. Do **not** embed the Settings text in the `reason` string — that reverts to
LLM-dependent parsing.

```python
# Good: structured
self._emit_event_callback(
    "my_new_event",
    {"setpoint_f": 72.0, "mode": "cool", "trigger": "user_schedule"},
)

# Bad: prose in reason
self._emit_event_callback(
    "my_new_event",
    {"reason": "set to 72F cool because user schedule"},
)
```

### Step 2 — Add an `EVENT_RENDERERS` entry (optional but recommended)

In `ai_skills_context.py`, add an entry to `EVENT_RENDERERS`:

```python
EVENT_RENDERERS: dict[str, Callable[[dict, str], tuple[str, str]]] = {
    ...
    "my_new_event": lambda data, unit: (
        f"My new event — {data.get('trigger', 'unknown')}",
        f"setpoint: {format_temp(data['setpoint_f'], unit)} {data['mode'].title()}",
    ),
}
```

If you omit this step, `_default_renderer` handles the type automatically. The renderer
is optional but provides a precise, stable display. Add it if the event is high-frequency
or user-visible.

### Step 3 — Add a catalog row to this document

Add a row to the appropriate group table in the [Event Catalog](#event-catalog) above:

```
| `my_new_event` | `my_emitter_file.py` `my_function` | `setpoint_f`, `mode`, `trigger` | "My new event — {trigger}" | `setpoint: {setpoint_f} {mode}` |
```

This is the single source of truth. The catalog row and the `EVENT_RENDERERS` entry must
agree on field names.

### Step 4 — Add a unit test case

In `tests/test_activity_renderers.py` (also corrected here — was previously misdocumented as
`test_ai_activity_table.py`, a name that doesn't exist in the repo), add a test that asserts:
- The Settings column is non-empty for your new event.
- The rendered text matches the expected format.

```python
def test_my_new_event_settings_populated():
    table = build_event_timeline_table(
        [{"time": "10:00", "type": "my_new_event", "setpoint_f": 72.0, "mode": "cool", "trigger": "user_schedule"}],
        config={"temp_unit": "fahrenheit"},
        hours=24,
        now=datetime.now(),
    )
    assert "72" in table
    assert "cool" in table.lower()
```

---

## Coverage Guardrail

`tests/test_activity_renderers.py`'s `TestEventRenderersCoverage` class contains a
**coverage-guardrail test** (corrected here — previously misdocumented as living in a
`test_ai_activity_table.py` file that doesn't exist in the repo) that:

1. Introspects `automation.py` and `coordinator.py` source text for every
   `_emit_event(_callback)?("type"` literal using a regex.
2. Asserts each discovered type is either present as a key in `EVENT_RENDERERS`, or
   otherwise explicitly accounted for as intentionally using `_default_renderer`.

If a developer adds a new `_emit_event_callback("my_new_type", ...)` call without
updating `EVENT_RENDERERS`, this test fails loudly.

The guardrail also asserts that `_default_renderer` produces:
- A non-empty Event string for a synthetic unknown type.
- A non-crashing Settings string for a payload with arbitrary fields.

**This test exists to prevent the recurring defect (Issues #206, #216, #290, #329, #330)
where new event types produced blank or LLM-dependent rows in the activity table.**

---

## Code Reference

- `build_event_timeline_table` — `ai_skills_context.py` (deterministic table builder; moved from `ai_skills_activity.py`, Issue #563)
- `_format_band_setpoint` — `ai_skills_context.py` (single-setpoint formatter)
- `EVENT_RENDERERS` — `ai_skills_context.py` (registry dict)
- `_default_renderer` — `ai_skills_context.py` (fallback renderer)
- `build_activity_timeline_context` — `ai_skills_context.py` (the context-provider wrapper the merged investigator skill calls; new in Issue #563)
- Coverage-guardrail test — `tests/test_activity_renderers.py` (`TestEventRenderersCoverage`)
- Band emission — `automation.py` `_apply_comfort_band` (~L1241)
- Override detection — `automation.py` `handle_thermostat_state_change` (~L817)
- Grace lifecycle — `automation.py` `_start_grace_period` (~L2460), `_on_grace_expired` (~L2465)
- Nat-vent events — `automation.py` `handle_door_window_opened` (~L1700), `_check_nat_vent_conditions` (~L1875), `_check_nat_vent_cycling` (~L2090)
- System events — `coordinator.py` `async_restore_state` (~L718), `_async_update_data` (~L1494)
